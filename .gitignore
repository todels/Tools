node_modules/
Client logos/
.DS_Store

# Root-level scratch files only — anchored so subprojects (font-sync/) can
# commit their own package.json / .mjs sources.
/*.mjs
/package.json
/package-lock.json

# Local Font Watcher config — contains a Supabase session token.
.fontsync/
font-sync/**/config.json
font-sync/**/state.json
